@model IEnumerable<StoreMenegment.StoreService.Helper.Futer>


<div style="display:flex">
    <div style="width:50%; ">
        @for (int i = 1; i <= 4; i++)
        {

<div>
    @Model.SingleOrDefault(m => m.FuterParentId == i).FuterName
</div>}
    </div>
    <div>
        @for (int i = 5; i <= 8; i++)
        {

<div>
    @Model.SingleOrDefault(m => m.FuterParentId == i).FuterName
</div>}

    </div>
</div>